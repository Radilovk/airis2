# Директни Редакторски Инструменти на Карти - Имплементация

## Дата: 13 Ноември 2024

## Обзор на Промените

Добавена е нова функционалност за директни редакторски инструменти на всяка карта в репорт страницата, когато Editor Mode е активен.

## Създадени Нови Файлове

### 1. `/src/components/report/CardEditorToolbar.tsx`
**Цел**: Компонент за редакторския панел на всяка карта

**Функционалности**:
- Компактен режим (бутон с 3 точки) и некомпактен режим (всички бутони видими)
- Контрол на видимост (скрий/покажи карта)
- Контрол на разгънато/свито състояние
- Система за коментари с timestamp и статус (решен/нерешен)
- Възможност за изтриване на карта
- Badge индикатор за брой нерешени коментари
- Popover меню за компактен режим
- Dialog прозорец за управление на коментари

**Експорти**:
- `CardEditorToolbar` - основният компонент
- `CardEditorState` - TypeScript интерфейс за състоянието
- `CardComment` - TypeScript интерфейс за коментар

### 2. `/src/components/report/withCardEditor.tsx`
**Цел**: HOC (Higher Order Component) и wrapper компонент за автоматично добавяне на редакторски инструменти

**Функционалности**:
- `withCardEditor<P>()` - HOC за обвиване на съществуващи компоненти
- `EditableCard` - готов wrapper компонент за Card елементи
- Автоматична интеграция с Editor Mode от admin config
- Persistence на състоянието чрез useKV hook
- Визуални ефекти (ring, hover) в editor mode
- Placeholder за скрити карти в editor mode
- AnimatePresence за smooth transitions

**Експорти**:
- `withCardEditor` - HOC функция
- `EditableCard` - wrapper компонент

### 3. `/src/components/report/tabs/OverviewTabWithEditor.tsx`
**Цел**: Пример за имплементация на Overview таб с editable карти

**Функционалности**:
- Пълна имплементация на Overview таб
- Всяка карта е обвита в `EditableCard`
- Unique card IDs за всяка секция:
  - `overview-health-score` - Общ Здравен Резултат
  - `overview-biometric` - Биометрични Данни
  - `overview-lifestyle` - Начин на Живот
  - `overview-goal-analysis` - Анализ на Постижимост на Цели
  - `overview-system-scores` - Резултати по Системи
  - `overview-health-progress` - Очакван Прогрес
- Запазена цялата функционалност на оригиналния таб
- Collapsible секции с интерактивност

### 4. `/CARD_EDITOR_GUIDE.md`
**Цел**: Пълна документация на функционалността

**Съдържание**:
- Детайлно описание на всички функции
- API документация
- Примери за използване
- Технически детайли за state management
- Troubleshooting секция
- Roadmap за бъдещи подобрения

### 5. `/CARD_EDITOR_QUICK_START.md`
**Цел**: Бърз старт гайд

**Съдържание**:
- Кратки инструкции за активиране
- Основни примери
- Визуална референция
- Workflow за редактори
- Инструкции за AI асистента

## Модифицирани Файлове

### `/src/components/screens/ReportScreen.tsx`
**Промени**:
- Добавен импорт на `OverviewTabWithEditor`
- Променена логиката в `renderModuleContent()` да използва новия таб когато Editor Mode е активен
- Запазена backwards compatibility с останалите табове

## Технически Детайли

### State Management
- **KV Store Key**: `'card-editor-states'`
- **Формат**: `Record<cardId, CardEditorState>`
- **Persistence**: Автоматично чрез `useKV` hook
- **Scope**: Global - споделено между всички компоненти

### CardEditorState Interface
```typescript
interface CardEditorState {
  id: string
  visible: boolean
  expanded: boolean
  comments: CardComment[]
  customStyles?: {
    backgroundColor?: string
    borderColor?: string
    padding?: string
  }
}
```

### CardComment Interface
```typescript
interface CardComment {
  id: string
  text: string
  timestamp: string
  resolved: boolean
}
```

## Визуални Подобрения

### В Editor Mode
- ✨ Ring ефект около картите: `ring-2 ring-primary/20`
- 🎯 Hover ефект: `hover:ring-primary/40`
- 📌 Компактен бутон с 3 точки в горен десен ъгъл
- 🔴 Badge с брой нерешени коментари
- 👻 Placeholder за скрити карти (border-dashed, opacity-40)

### Animations
- Smooth fade in/out при скриване/показване
- Scale animation при mount на toolbar
- Slide animations в comment dialog

## Използване

### За Редактори/Админи
1. Активирай Editor Mode от Admin Panel
2. Всяка карта показва редакторски инструменти
3. Добавяй коментари с инструкции
4. Контролирай видимост и expand state
5. Маркирай коментари като решени

### За Разработчици
```tsx
import { EditableCard } from '@/components/report/withCardEditor'

<EditableCard cardId="my-unique-card" editorMode={editorMode}>
  <Card>
    <CardHeader><CardTitle>Заглавие</CardTitle></CardHeader>
    <CardContent>Съдържание...</CardContent>
  </Card>
</EditableCard>
```

## Backwards Compatibility

- ✅ Съществуващите табове работят без промени
- ✅ Editor Mode остава опционален
- ✅ Всички стари функционалности запазени
- ✅ Новата функционалност не засяга нормалния потребителски изглед

## Performance Considerations

- Минимален overhead при disabled Editor Mode
- Lazy loading на toolbar компоненти
- Efficient re-renders само при промяна на card state
- useKV за persistence без допълнителни API calls

## Бъдещи Подобрения (Roadmap)

1. **Drag & Drop** - преподреждане на карти
2. **Visual Style Editor** - промяна на цветове, padding
3. **Export Comments** - изтегляне на коментари като CSV/JSON
4. **History** - проследяване на промените
5. **Versioning** - запазване на версии на карти
6. **Templates** - готови шаблони за карти
7. **Bulk Operations** - операции върху множество карти
8. **Filtering** - филтриране на карти по коментари/статус

## Testing Notes

- Тествано в Editor Mode и нормален режим
- Проверена persistence между refreshes
- Тествана интерактивност на всички бутони
- Проверени edge cases (липсващи props, undefined states)

## Dependencies

Използвани съществуващи зависимости:
- `@radix-ui/react-popover` - за popover меню
- `@radix-ui/react-dialog` - за comment dialog
- `framer-motion` - за animations
- `@phosphor-icons/react` - за иконите
- Shadcn UI компоненти (Button, Badge, Textarea, Card)

## Breaking Changes

❌ Няма breaking changes - всички промени са backwards compatible

## Migration Guide

Не е необходима миграция. Новата функционалност е опционална и може да се добавя постепенно към съществуващите табове.

## Примери за Интеграция

### Минимален Пример
```tsx
<EditableCard cardId="simple-card" editorMode={true}>
  <Card>Content</Card>
</EditableCard>
```

### С Всички Опции
```tsx
<EditableCard 
  cardId="advanced-card"
  editorMode={editorMode}
  defaultVisible={true}
  defaultExpanded={false}
  compact={true}
  position="top-right"
  onDelete={(id) => handleDelete(id)}
>
  <Card>Content</Card>
</EditableCard>
```

### HOC Pattern
```tsx
const MyEditableChart = withCardEditor(MyChartComponent)

<MyEditableChart 
  cardId="chart-1"
  editorMode={true}
  data={chartData}
  title="My Chart"
/>
```

## Notes

- Card IDs трябва да бъдат уникални в рамките на приложението
- Препоръчителен naming pattern: `{tab}-{section}-{subsection}`
- Примери: `overview-health-score`, `iridology-left-zones`, `plan-nutrition-recommendations`
